class contato{
  String nome;
  int idade;
  String telefone;
  String email;
  bool colorado;
  contato(this.nome, this.idade, this.telefone, this.email, [this.colorado=true]); //construtor []=opcional
}